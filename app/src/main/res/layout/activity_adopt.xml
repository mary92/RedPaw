<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".AdoptActivity">

    <include
        android:id="@+id/adopt_toolbar"
        layout="@layout/toolbar"/>

    <EditText
        android:id="@+id/adopt_fieldSearch"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/adopt_toolbar"
        android:layout_toLeftOf="@+id/adopt_btnSearch"
        android:background="#ffffff"
        android:layout_margin="8dp"
        android:padding="5dp"
        android:hint="Search term"
        android:text=""/>

    <Button
        android:id="@+id/adopt_btnSearch"
        android:layout_below="@id/adopt_toolbar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:text="Search"/>

    <ListView
        android:id="@+id/adopt_listViewResults"
        android:layout_width="fill_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="8dp"
        android:layout_below="@id/adopt_fieldSearch">
    </ListView>

    <Button
        android:id="@+id/adopt_btnMap"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:text="View Map"/>


</RelativeLayout>
